package babylonjs;

typedef ClientRect =
{
    var bottom: Float;
    var left: Float;
    var right: Float;
    var top: Float;
    
    var width(default, never): Float;
    var height(default, never): Float;
}