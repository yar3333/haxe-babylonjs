package babylon;

extern class DebugLayer
{
	private var _scene : Dynamic/*UNKNOW_TYPE*/;
	private var _camera : Dynamic/*UNKNOW_TYPE*/;
	private var _transformationMatrix : Dynamic/*UNKNOW_TYPE*/;
	private var _enabled : Dynamic/*UNKNOW_TYPE*/;
	private var _labelsEnabled : Dynamic/*UNKNOW_TYPE*/;
	private var _displayStatistics : Dynamic/*UNKNOW_TYPE*/;
	private var _displayTree : Dynamic/*UNKNOW_TYPE*/;
	private var _displayLogs : Dynamic/*UNKNOW_TYPE*/;
	private var _globalDiv : Dynamic/*UNKNOW_TYPE*/;
	private var _statsDiv : Dynamic/*UNKNOW_TYPE*/;
	private var _statsSubsetDiv : Dynamic/*UNKNOW_TYPE*/;
	private var _optionsDiv : Dynamic/*UNKNOW_TYPE*/;
	private var _optionsSubsetDiv : Dynamic/*UNKNOW_TYPE*/;
	private var _logDiv : Dynamic/*UNKNOW_TYPE*/;
	private var _logSubsetDiv : Dynamic/*UNKNOW_TYPE*/;
	private var _treeDiv : Dynamic/*UNKNOW_TYPE*/;
	private var _treeSubsetDiv : Dynamic/*UNKNOW_TYPE*/;
	private var _drawingCanvas : Dynamic/*UNKNOW_TYPE*/;
	private var _drawingContext : Dynamic/*UNKNOW_TYPE*/;
	private var _rootElement : Dynamic/*UNKNOW_TYPE*/;
	private var _skeletonViewers : Dynamic/*UNKNOW_TYPE*/;
	var _syncPositions : Void->Void;
	private var _syncData : Dynamic/*UNKNOW_TYPE*/;
	private var _syncUI : Dynamic/*UNKNOW_TYPE*/;
	private var _onCanvasClick : Dynamic/*UNKNOW_TYPE*/;
	private var _clickPosition : Dynamic/*UNKNOW_TYPE*/;
	private var _ratio : Dynamic/*UNKNOW_TYPE*/;
	private var _identityMatrix : Dynamic/*UNKNOW_TYPE*/;
	private var _showUI : Dynamic/*UNKNOW_TYPE*/;
	private var _needToRefreshMeshesTree : Dynamic/*UNKNOW_TYPE*/;
	var shouldDisplayLabel : Node->Bool;
	var shouldDisplayAxis : Mesh->Bool;
	var axisRatio : Float;
	var accentColor : String;
	var customStatsFunction : Void->String;
	function new(scene:Scene) : Void;
	private function _refreshMeshesTreeContent();
	private function _renderSingleAxis(zero, unit, unitText, label, color);
	private function _renderAxis(projectedPosition, mesh, globalViewport);
	private function _renderLabel(text, projectedPosition, labelOffset, onClick, getFillStyle);
	private function _isClickInsideRect(x, y, width, height);
	function isVisible() : Bool;
	function hide() : Void;
	private function _clearSkeletonViewers();
	function show(?showUI:Bool, ?camera:Camera, ?rootElement:HtmlElement) : Void;
	private function _clearLabels();
	private function _generateheader(root, text);
	private function _generateTexBox(root, title, color);
	private function _generateAdvancedCheckBox(root, leftTitle, rightTitle, initialState, task, ?tag);
	private function _generateCheckBox(root, title, initialState, task, ?tag);
	private function _generateButton(root, title, task, ?tag);
	private function _generateRadio(root, title, name, initialState, task, ?tag);
	private function _generateDOMelements();
	private function _displayStats();
}